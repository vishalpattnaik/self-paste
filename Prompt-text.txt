@workspace I need to update my CSV parsing logic to respect the "order" column so recipes execute deterministically.

Please modify the highlighted code with these exact changes:
1. Update the `RecipeRow` record to include `int order` as its first parameter.
2. In the `loadRecipes()` method, inside the `for (CSVRecord row : csvParser)` loop, extract the order column using `Integer.parseInt(row.get("order").trim())`.
3. Pass this parsed order integer into the new `RecipeRow` constructor.
4. After the `while` loop finishes building the `rows` list, sort the list by the `order` property in ascending order before returning it (e.g., `rows.sort(Comparator.comparingInt(RecipeRow::order))`).
