We recently changed the OpenRewrite recipe collection from a List<String> to a Set<String> (to deduplicate recipes), but now the code fails to compile because the downstream execution method or Maven command builder is still strictly expecting a List<String>.

Please fix the type mismatch by doing one of the following:
1. Update the downstream method signature to accept a `Collection<String>` or `Set<String>` instead of a `List<String>`.
2. OR, if the downstream method cannot be changed, wrap the deduplicated Set back into a List right before passing it to the executor (e.g., `new ArrayList<>(deduplicatedRecipeSet)`).
