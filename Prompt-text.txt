@workspace We need to fix an LLM hallucination in the reporting workflow. The agent is currently ignoring "Skipped" or "Failed" responses from the POM modification tool and incorrectly injecting those dependencies into the `directUpgradesJson` payload for the `generateExcelReport` tool.

Please update the Java code to enforce a strict contract:
1. Update the POM modification tool to ensure its return string always starts with a strict keyword: either "SUCCESS:" or "SKIPPED:".
2. Modify the description/documentation string of the `generateExcelReport` MCP tool to include this exact instruction:
"CRITICAL ROUTING RULE: You must strictly evaluate the return string from the POM modification tool. If the response starts with 'SUCCESS:', place the dependency in the directUpgradesJson array. If the response starts with 'SKIPPED:' or indicates any failure (e.g., identical version, Artifactory miss, framework block), you MUST place that dependency into the manualReviewJson array. Do not assume success."
