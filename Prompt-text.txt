@workspace Look at my highlighted Java method. It currently executes a Maven build using `clean install`.
Goal: Change the Maven execution to use `clean test` and make it run much faster so it doesn't time out.
Rules:
1. Replace the "install" argument with "test".
2. Add the argument "-T" followed by "1C" to enable parallel thread execution.
3. Add the argument "-Dsurefire.skipAfterFailureCount=1" to make the test suite fail fast.
4. Ensure the `ProcessBuilder` (or execution logic) correctly separates these arguments.
5. Keep all existing error handling, output stream reading, and working directory logic exactly the same.



