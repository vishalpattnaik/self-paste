@workspace We are finalizing the transition to a pure Maven Plugin. Please make the following exact changes to the `pom.xml`:

1. Change the `<artifactId>` to exactly: `cve-maven-plugin`. (This is critical to unlock the 'cve' command prefix).
2. Ensure `<packaging>maven-plugin</packaging>` is set.
3. Completely DELETE the `spring-boot-maven-plugin` block from the `<build><plugins>` section. We do not want to build an executable executable Fat JAR anymore; we need a standard library JAR.
4. Ensure `maven-plugin-api` and `maven-plugin-annotations` are still present in the dependencies.
