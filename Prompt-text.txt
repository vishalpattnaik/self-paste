@workspace The dependency bumper tool is accidentally downgrading dependencies because it blindly applies the "fixed version" from the offline database, even if the project's current version is already newer.

Update the replacement logic to explicitly prevent downgrades:
1. Import `org.apache.maven.artifact.versioning.ComparableVersion`. (Add maven-artifact to the pom if necessary).
2. Before modifying the pom.xml, parse both the `currentVersion` (extracted from the POM) and the `suggestedNewVersion` using `ComparableVersion`.
3. Compare them. If `suggestedNewVersion.compareTo(currentVersion) <= 0`, abort the replacement.
4. Return a failure payload stating: "Skipped: The suggested fix version is a downgrade or identical to the current version. This is likely a scanner false positive."
