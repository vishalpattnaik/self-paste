2. **STRICT DIRECT DEPENDENCY RULE:** For the remaining vulnerabilities lacking a CSV recipe, categorize them by strictly checking the project's local `pom.xml`:
   - **Direct:** The dependency is explicitly written in the `pom.xml`.
   - **Transitive/Managed:** The dependency is NOT explicitly written in the `pom.xml` (inherited from the enterprise parent POM or another library).
3. **Filter and Execute:** 
   - You MUST ONLY execute the `autoBumpDependencies` MCP tool for **Direct** dependencies. 
   - You MUST NEVER execute tools for **Transitive/Managed** dependencies. Do not attempt to add them to `<dependencyManagement>`. Immediately flag them as "SKIPPED: Transitive dependency - must be resolved via core parent framework" and place them directly in the "Requires Manual Review" table.




1. **STRICT DIRECT DEPENDENCY RULE:** If any vulnerable dependencies map to a recipe in the CSV, verify they are DIRECT dependencies explicitly written in the local `pom.xml`. If they are transitive/managed, SKIP the recipe entirely and move them to Manual Review. If they are Direct, gather the recipe names.
