@workspace Locate all methods in my service classes that were previously exposed to the AI (e.g., parsing the Nexus report, auto-bumping dependencies, running OpenRewrite, generating Excel).

1. Remove any AI-specific annotations (like `@Tool` or `@Description`).
2. Ensure these methods are standard `public` methods inside their respective `@Service` classes.
3. Update their return types. Instead of returning formatted Markdown strings or Tool response payloads for an LLM, modify them to return standard Java objects (like `List<Vulnerability>`, `boolean` for success, or custom DTOs) so they can pass data directly to each other in Java memory.
