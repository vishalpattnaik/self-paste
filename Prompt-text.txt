@workspace My HTML dashboard is being deleted by Maven's `clean` phase because it is being generated inside the `target/` directory. I need to move it to the root of the scanned project.

1. Find the main orchestration method where `HtmlReportGenerator.generateOfflineDashboard()` is called.
2. Look at the `outputPath` argument being passed to it.
3. Change the output path so it no longer includes `/target/`. It should resolve directly to the root of the project path. For example, if you were doing `projectPath + "/target/vulnerability-dashboard.html"`, change it to `projectPath + "/vulnerability-dashboard.html"`.
4. Update the console log message to reflect that the file is now saved in the project root.

Show me the updated method call.
