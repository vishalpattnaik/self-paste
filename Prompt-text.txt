@workspace The data in my generated HTML dashboard is wrong. The "Autonomously Upgraded & Remediated" table is showing all the transitive and unfixable dependencies, which means the wrong lists are being passed to `HtmlReportGenerator.generateOfflineDashboard()`.

1. Go to the method where `HtmlReportGenerator.generateOfflineDashboard()` is called.
2. Check the two list arguments being passed into it. 
3. The FIRST list argument must ONLY contain the dependencies that were successfully bumped in the pom.xml or successfully processed by OpenRewrite.
4. The SECOND list argument must contain the "leftovers" (the transitive dependencies, the false positives, or the ones that failed to update).
5. Fix the variable references being passed into the method so the tables render correctly.

Show me the corrected method call.
