Review the `autoBumpDependencies` method. There are two critical logic leaks we need to fix regarding the gatekeeper and XML injection routing.

Update the method with these strict rules:
1. **Wire up the Gatekeeper:** At the very beginning of the method, you MUST call `isSafeToAutoBump(groupId)`. If it returns false, immediately abort the modification and return: "Skipped: Major Framework. Requires parent POM upgrade."
2. **Direct vs. Transitive Routing:** Do not force everything into `<dependencyManagement>`. Before modifying the POM, check if the dependency already exists explicitly within the main `<dependencies>` block of the `pom.xml`.
3. **Direct Update:** If it is a Direct dependency (found in `<dependencies>`), use Regex to update its `<version>` tag exactly where it currently sits.
4. **Transitive Update:** If it is a Transitive dependency (NOT found in `<dependencies>`), then and only then should you use Regex to inject or update it inside the `<dependencyManagement><dependencies>` block.
5. **Preserve Formatting:** Continue using the `java.util.regex` String replacement approach. Do not revert to `javax.xml` DOM parsers.
