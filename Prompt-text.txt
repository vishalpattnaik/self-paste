@workspace My `HtmlReportGenerator.generateOfflineDashboard()` didn't run in the last execution. I need to guarantee it executes at the absolute end of the scanning process, regardless of early returns or Maven clean phases.

1. Find the main MCP scanning method (the one that returns the final string to the AI agent).
2. Wrap the core scanning, filtering, and remediation logic inside a `try { ... }` block.
3. Add a `finally { ... }` block at the very end of the method.
4. Move the `HtmlReportGenerator.generateOfflineDashboard(...)` call INSIDE the `finally` block. 
5. Ensure the variables passed to the generator (the projectName, the successfully fixed list, and the manual review list) are declared outside/above the `try` block so they are accessible in the `finally` block.

Show me the updated orchestration method with the try-finally structure.
