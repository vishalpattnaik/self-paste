@workspace I am refactoring this application from an MCP Server/Web API into a standalone Interactive Java CLI tool. 

Please perform the following cleanup:
1. Open the `pom.xml`. Remove any dependencies related to `spring-boot-starter-web`, `spring-boot-starter-webflux`, `spring-ai`, or MCP (Model Context Protocol).
2. Ensure `spring-boot-starter` is still present so we can use Core Spring DI, but add `<properties><spring-boot.mainclass>...</spring-boot.mainclass></properties>` if necessary.
3. Delete the configuration classes related to MCP (e.g., `WebMvcSseServerTransportProvider`, or any `@RestController` handling AI endpoints).
4. Do not touch the existing Excel generation, Maven process execution, or JSON parsing logic. We still need those.
