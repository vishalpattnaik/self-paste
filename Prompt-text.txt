@workspace I want to change this Mojo from accepting a manual recipe string to acting as an autonomous tech-debt upgrade engine.

Please modify this class with these exact changes:
1. Delete the `@Parameter(property = "recipe", required = true) private String recipeName;` field.
2. In the `execute()` method, remove the log statement that prints the `recipeName`.
3. Change the execution call from `rewriteService.executeRecipe(projectBaseDir.getAbsolutePath(), recipeName);` to a new method we are about to create: `boolean success = rewriteService.executeProactiveUpgrades(projectBaseDir.toPath());`
4. Update the log messages to reflect an "Autonomous Ordered Tech-Debt Upgrade" rather than a manual recipe target.



@workspace I need to add an autonomous, deterministic upgrade engine to this service that matches project dependencies against our ordered CSV catalog.

Please add the following methods to this class:
1. Add a private helper method: `private Set<String> extractArtifactIdsFromPom(Path pomPath)`. 
   - Inside this method, use standard Java `javax.xml.parsers.DocumentBuilderFactory` to parse the `pom.xml` file.
   - Extract the text content of all `<artifactId>` tags across the entire document.
   - Convert them to lowercase and return them as a `Set<String>`. Catch and log any XML parsing exceptions.
2. Add a public method: `public boolean executeProactiveUpgrades(Path projectPath)`.
   - Resolve the "pom.xml" path. If it doesn't exist, log an error and return false.
   - Call `extractArtifactIdsFromPom` to get the list of artifacts currently used in the project.
   - Call our existing `loadRecipes()` method to get the list of `RecipeRow` objects (which is already sorted by the `order` column).
   - Iterate through the sorted `RecipeRow` list. 
   - For each row, check if the `artifactId` Set contains `row.artifactName()`.
   - If it does, log an info message (e.g., "[MATCH] Found matching artifact '{name}'. Queuing upgrade recipe: {recipe}").
   - Call our existing `runSingleRecipe(projectPath, row.recipeName())` method.
   - Keep track of whether any changes were made and return a final boolean indicating if any upgrades were successfully applied.
