@workspace Now, implement the Interactive REPL terminal. 

1. Modify the main Spring Boot application class (the one annotated with `@SpringBootApplication`).
2. Make it implement `CommandLineRunner`.
3. Inject the `SecurityPipelineOrchestrator` via constructor.
4. In the `run(String... args)` method, implement a continuous `while(true)` loop using `java.util.Scanner(System.in)`.
5. Print a welcome banner. 
6. Prompt the user: "security-cli> Enter the absolute path to the project to scan (or type 'exit' to quit):".
7. If the user types 'exit', `break` the loop and let the application shut down cleanly.
8. Validate that the input is a valid directory path. If invalid, print an error and `continue` the loop.
9. If valid, pass the path to `securityPipelineOrchestrator.runPipeline(input)`. 
10. Catch all exceptions inside the loop, print the error message, and ensure the loop continues so the application stays awake for the next project.
