 I want to include the name of the service in the generated Excel file's name.
Goal: Extract the folder name from the `targetDirectory` and prepend it to the Excel filename.
Rules:
1. Parse the `targetDirectory` string to get just the final folder name (e.g., if the path is `/some/path/my-service`, extract "my-service"). You can use `Paths.get(targetDirectory).getFileName().toString()`.
2. Construct the new file name using that folder name (e.g., `String fileName = folderName + "-remediation-report.xlsx";`).
3. If you previously added a timestamp to the file name, keep the timestamp, just add the folder name to the front.
4. Keep all existing Excel generation logic exactly the same.
