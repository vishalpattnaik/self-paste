@workspace We need to fix a critical performance bug in the OpenRewrite orchestration logic. Currently, if multiple Spring dependencies (like spring-boot-starter-web and spring-boot-starter-data-jpa) are flagged for CVEs, the engine adds the same umbrella recipe (e.g., UpgradeSpringBoot_3_0) to a List, causing the heavy AST parser to execute redundantly.

Please refactor the recipe collection logic:
1. Change the collection structure from a `List<String>` to a `Set<String>` (specifically a `LinkedHashSet` if we need to preserve insertion order) to automatically deduplicate the recipes.
2. Ensure that the final Maven command execution for OpenRewrite only receives this deduplicated set of recipes, guaranteeing that each unique recipe runs exactly once per build.
