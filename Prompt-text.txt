**Step 2: Fallback to Standard Scan (SBOM)**
- Check if the SBOM file exists at `target/bom.json`.
- **IF NOT FOUND:** You MUST explicitly invoke CMD to bypass PowerShell parsing constraints. Run this exact command string:
  cmd /c "mvn cyclonedx:makeAggregateBom"
- **CRITICAL ASYNC WAIT RULE:** File system I/O takes a few seconds. After the terminal reports `BUILD SUCCESS`, DO NOT immediately abort if `target/bom.json` is missing. 
- You MUST poll and verify the file exists. If it is not found on the first check, wait a moment and check the `target` directory again. You must retry this file check at least 3 times before declaring a configuration failure.
- Only proceed to call the `scanSbomFile` tool once you have positively confirmed `target/bom.json` is physically present on disk.
