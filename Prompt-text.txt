I want to prevent the heavy offline NVD database from loading when running this manual upgrade goal. 

Please modify the highlighted `execute()` method to do the following:
1. Immediately before booting the `SpringApplicationBuilder`, add `System.setProperty("nvd.init.skip", "true");`.
2. Wrap the entire Spring application runtime context lifecycle (the `try-with-resources` block) in a standard `try-finally` block.
3. Inside the `finally` block, ensure that the system property is cleanly removed using `System.clearProperty("nvd.init.skip");` to avoid leaking the configuration to subsequent Maven execution threads.
