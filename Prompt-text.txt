@workspace Review this exception handler method. It is currently causing a fatal `HttpMessageNotWritableException` when an error occurs on the MCP Server-Sent Events (SSE) stream because Spring cannot serialize a `Map` to a `text/event-stream` Content-Type.

Please update the method with this routing logic:
1. Inject `HttpServletRequest request` into the method signature if it is not already present.
2. Read the request path using `request.getRequestURI()`.
3. Add a conditional check: If the path contains `/sse`, `/messages`, or matches our MCP endpoint, DO NOT return a `Map`. Instead, return a plain `String` formatted as: `"Internal Server Error: " + e.getMessage()`.
4. If the path does not match the SSE endpoints, fall back to the existing logic and return the standard JSON `Map` or `ResponseEntity<Map>`.
5. Ensure the return type of the method is broad enough (like `Object` or `ResponseEntity<?>`) to accommodate returning both Strings and Maps depending on the request type.
