I want to intercept the "nvd.init.skip" system property to bypass the database initialization routine during manual framework upgrades.

Please modify the highlighted code with these changes:
1. Add a new private boolean field named `skipNvdInit`. Use the Spring `@Value` annotation to map it to the property: `@Value("${nvd.init.skip:false}")`.
2. Navigate to the very first line of the initialization method (the method marked with `@PostConstruct` that handles loading or building the SQLite database).
3. Insert a guard condition at the top: if `skipNvdInit` is true, log an info message like "[FAST BOOT] Bypassing NVD Database initialization for manual upgrade mode." and execute an immediate return statement to exit the method before any database connection or processing occurs.
