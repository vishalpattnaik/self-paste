@workspace I need to replace the ProcessBuilder logic in `runSingleRecipe` with the native OpenRewrite in-memory Java API.

Please rewrite this highlighted method completely with the following implementation:
1. Keep the method signature exactly the same: `private boolean runSingleRecipe(Path projectPath, String recipeName)`.
2. Construct the path to the target pom: `Path targetPomPath = projectPath.resolve("pom.xml");` and return false if it doesn't exist.
3. Initialize an `InMemoryExecutionContext`: `ExecutionContext ctx = new InMemoryExecutionContext(t -> log.error("OpenRewrite error: ", t));`
4. Load the recipe from the classpath: `Recipe recipe = Environment.builder().scanRuntimeClasspath().build().activateRecipes(recipeName);`
5. Parse the target pom.xml into a List of SourceFiles: `List<SourceFile> parsedPom = MavenParser.builder().build().parse(Collections.singletonList(targetPomPath), projectPath, ctx).toList();`
6. Execute the recipe: `RecipeRun recipeRun = recipe.run(parsedPom, ctx);`
7. Iterate through `recipeRun.getResults()`. If a result contains changes (`result.getAfter() != null`), write the modified string (`result.getAfter().printAll()`) back to `targetPomPath` using `Files.writeString()`.
8. Return `true` if changes were written, or `false` if `recipeRun.getResults()` was empty. Use your existing `log.info` and `log.error` structures to wrap this in a try-catch block.
