Fix the `generateExcelReport` tool's column sizing logic.
Goal: Prevent the Apache POI 255-character column width crash.
Rules:
1. Wherever you are calculating the column width or using `autoSizeColumn`, add a strict mathematical cap.
2. The width must never exceed 255 (or whatever unit POI requires, usually `Math.min(calculatedWidth, 255)`). 
3. Ensure any null values from the JSON parsing are handled gracefully as empty strings `""` before measuring their length.



2. Convert the data from your Section 1 and Section 2 tables into three valid JSON arrays.
3. **CRITICAL JSON SCHEMA RULE:** You MUST format the JSON arrays using strict camelCase keys, regardless of the markdown headers. 
   - Use exactly these keys: `dependency`, `currentVersion`, `originalVersion`, `newVersion`, `cves`, `order`, `recipe`. DO NOT use spaces or PascalCase in the JSON keys.
4. Pass these JSON arrays as arguments, and pass the absolute file path of the current VS Code workspace as the `targetDirectory` parameter.
