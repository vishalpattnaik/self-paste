@workspace I need to add a strict heuristic gatekeeper to prevent major architectural frameworks from being auto-bumped.

1. Add a `private static final List<String> MAJOR_FRAMEWORKS` containing these exact Regex patterns:
   - "^org\\.springframework.*"
   - "^org\\.apache\\.tomcat.*"
   - "^io\\.netty.*"
   - "^io\\.grpc.*"
   - "^org\\.eclipse\\.jetty.*"
   - "^org\\.hibernate.*"
2. Create a public method `boolean isSafeToAutoBump(String groupId)`.
3. The method should evaluate the `groupId` against the `MAJOR_FRAMEWORKS` list. Return `false` if it matches any of the regex patterns (meaning it is a major framework), and `true` if it matches none (meaning it is a safe standalone utility).
