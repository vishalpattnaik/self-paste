@workspace I noticed a gap in the `SecurityPipelineOrchestrator`. Currently, it only looks for `nexus.json`. My original pipeline had a fallback mechanism to read CycloneDX SBOMs if the Nexus report was missing.

Please update the `runPipeline(String targetProjectPath)` method to implement this fallback logic:
1. Define both potential report paths: 
   - `File nexusFile = new File(targetProjectPath, "target/nexus.json");` (or check for files matching `nexus*.json`).
   - `File sbomFile = new File(targetProjectPath, "target/bom.json");`
2. Implement an `if/else` check:
   - IF the Nexus file exists, call the Nexus parsing service to extract the CVEs.
   - ELSE IF the Nexus file is missing but `bom.json` exists, call the existing SBOM parsing service to extract the CVEs.
   - ELSE throw a clear `FileNotFoundException` or log an error stating: "[ERROR] Neither nexus.json nor bom.json found in the target directory." and `return` early so the REPL loops back to the prompt.
3. Ensure the extracted CVE list from either source flows seamlessly into the next step (the Direct Dependency Filter).
