### Step 2: Fallback to Standard Scan (SBOM)
1. **Initial Search:** Search the workspace for `target/bom.json`.
2. **MANDATORY GENERATION:** If the file does not exist, this is expected behavior. DO NOT report an error and DO NOT ask the user for permission. You MUST immediately open the terminal and execute this exact command:
   `mvn cyclonedx:makeAggregateBom -B`
3. **Wait & Poll:** File system writing takes time. After the terminal reports BUILD SUCCESS, wait 5 seconds, then search for `target/bom.json` again. 
4. **Proceed:** Once you confirm the file is on disk, proceed immediately to the vulnerability scanning tools.
